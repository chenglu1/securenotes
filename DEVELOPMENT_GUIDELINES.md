# SecureNotes 开发规范

本规范适用于当前仓库的日常开发、评审和 AI 协作开发。后续新增功能、修复缺陷、重构和文档更新，默认都应遵循本规范。

## 1. 目标

1. 保持交付速度，同时控制回归风险。
2. 让 AI 负责高重复、高样板工作，人负责边界、取舍和验收。
3. 保证每次改动可理解、可验证、可回滚。

## 2. 基本原则

1. 先定义任务，再改代码。需求不清、边界不清、验收不清时，不直接进入实现。
2. 优先小步迭代，不做无约束的大改。
3. 优先沿用现有架构、目录和命名风格，不为了“更流行”引入新范式。
4. 非必要不改公共接口、数据库结构、全局样式和基础设施。
5. 不修顺手问题，不混入与当前任务无关的重构。
6. 所有改动都必须经过验证，而不是只看代码“像是对的”。

## 3. 标准协作流程

1. 写任务卡
   任务至少要包含：背景、目标、非目标、影响范围、验收标准、风险点。
2. 补上下文
   说明技术栈、相关文件、参考实现、禁止改动区域、验证方式。
3. 先出方案，再做实现
   涉及多个模块、数据库、接口、状态管理、权限或部署配置时，先确认方案。
4. 小步实现
   一次只解决一个明确问题；大任务拆成多个小任务或多个 PR。
5. 自检
   开发者先检查是否跑偏、是否超范围、是否引入无关变更。
6. 运行验证
   至少执行与本次改动相关的类型检查、构建、测试和关键路径人工验证。
7. 人工评审
   Review 重点看行为正确性、边界条件、兼容性、风险和验证充分性。
8. 合并
   通过验证和评审后再合并，必要时补充回滚说明。

## 4. 任务输入规范

每个开发任务默认补齐以下信息：

1. 背景：为什么要做。
2. 目标：最终要解决什么问题。
3. 非目标：本次不解决什么。
4. 影响范围：允许修改哪些模块、页面、接口、表结构。
5. 禁止事项：哪些区域不能动，哪些行为不能改变。
6. 验收标准：完成后以什么结果判定通过。
7. 验证要求：需要跑哪些命令、点哪些页面、看哪些状态。

## 5. 仓库架构边界

后续开发默认遵守以下边界：

1. Electron 保持 main / preload / renderer 分层，不把 Node 能力直接塞进 React 组件。
2. 渲染层访问本地能力时，优先通过 `window.api` 和 IPC，不绕过 preload 直接访问 Electron API。
3. 笔记列表与详情保持分离：列表走 summary，详情单独读取，不把完整正文重新耦合回侧栏列表。
4. 前端继续沿用 React + Zustand + Electron IPC 的现有模式，不额外引入新的全局状态方案。
5. 后端继续沿用 NestJS + TypeORM 的现有架构，不在未明确决策前引入 Prisma 或 Drizzle。
6. 认证接口继续保持在 `/api/auth`，笔记资源继续保持在 `/api/notes`，避免无收益的路由漂移。
7. 同步逻辑属于高风险区域，涉及事务、并发、版本推进和冲突处理时，必须谨慎设计和重点验证。

## 6. 编码规范

1. 优先最小改动，修改范围与任务目标严格对齐。
2. 保持现有代码风格和文件组织，不做与任务无关的格式化清洗。
3. 优先复用现有工具函数、组件、样式变量和模块边界。
4. 非必要不新增依赖；新增依赖必须说明收益和替代方案。
5. 注释只写必要信息，避免解释显而易见的代码。
6. 新增代码必须考虑空态、错误态、加载态和边界值。
7. 删除代码时，确认没有被其他模块隐式依赖。

## 7. 前端规范

1. UI 改动优先延续现有产品风格，不随意引入全新视觉语言。
2. 修改组件时，同时检查桌面端和紧凑布局下是否可用。
3. 交互改动应明确用户可感知变化，并补充必要截图或说明。
4. 列表、表单、弹窗等高频交互要覆盖加载、空数据、失败和成功状态。
5. 不为了单个页面问题去重写全局样式或公共组件，除非任务明确要求。

## 8. 后端规范

1. 优先沿用现有 Nest 模块边界，不为了单个需求平行搭一套新架构层。
2. DTO、Service、Module、Repository 的职责要清晰，不在 Controller 里堆业务逻辑。
3. 涉及事务、锁、同步游标、版本号时，先保证一致性，再追求写法简洁。
4. 接口变更默认优先兼容旧客户端；不兼容变更必须同步说明影响范围。
5. 数据结构变更必须配套迁移方案、验证方案和必要的回滚预案。

## 9. AI 协作规范

适合交给 AI 的工作：

1. CRUD、样板代码、DTO、类型定义。
2. 局部组件改造、样式微调、表单和列表逻辑。
3. 测试样例、文档补充、重复代码收敛。

不适合直接交给 AI 拍板的工作：

1. 核心架构迁移。
2. 权限模型、支付、计费、加密、安全边界。
3. 并发一致性方案、事务设计、数据库迁移策略。
4. 没有上下文的大规模重写。

AI 协作要求：

1. 大于单模块的改动，先出方案再实现。
2. AI 生成代码后必须人工 review，不接受盲信。
3. AI 改动也必须通过同样的验证门禁。
4. 提交说明里要写清楚改动目标、范围和验证结果。

## 10. 质量门禁

所有改动至少满足以下条件后才能合并：

1. 相关文件的类型检查通过。
2. 相关构建通过。
3. 相关测试通过；修 bug 时至少补一个回归测试，若当前没有测试框架则补人工验证说明。
4. 关键路径人工验证完成。
5. 没有明显超范围改动、死代码或未处理异常。

如涉及以下内容，需要附加检查：

1. 数据库变更：迁移可执行、历史数据可兼容、回滚可评估。
2. 接口变更：前后端兼容性、返回结构和错误分支都已确认。
3. UI 变更：截图或录屏对比，确认空态、错误态、加载态。
4. 同步逻辑变更：至少验证新增、更新、删除、冲突和重新登录场景。

## 11. Review 清单

评审时至少检查：

1. 是否真正满足任务目标。
2. 是否改出了任务范围外副作用。
3. 是否破坏现有架构边界。
4. 是否引入了重复实现或不必要依赖。
5. 是否处理了异常路径和边界值。
6. 是否有安全、权限、数据一致性风险。
7. 是否给出足够验证证据。

## 12. 提交与 PR 规范

1. 所有改动走分支和 PR，不直接改主分支。
2. 一个 PR 尽量只解决一个主题。
3. 提交信息和 PR 描述要写明“改了什么、为什么改、怎么验证”。
4. UI 改动补截图；接口和数据变更补影响说明。
5. 如果存在未覆盖风险，要在 PR 中显式写出来。

建议 PR 描述包含：

1. 变更概要。
2. 影响范围。
3. 验证结果。
4. 残余风险。

## 13. 安全与数据规范

1. 不把生产密钥、真实账号、令牌、敏感用户数据直接暴露给 AI 或写入仓库。
2. 涉及鉴权、上传下载、命令执行、数据库写入的改动要重点 review。
3. 涉及个人数据、同步数据、历史数据兼容的修改，必须显式说明影响。
4. 生产环境禁止依赖 `synchronize=true` 这类高风险默认行为，结构变更统一走 migration。

## 14. 例外处理

如需偏离本规范，必须至少说明：

1. 为什么当前规范不适用。
2. 替代方案是什么。
3. 风险如何控制。
4. 由谁确认。

## 15. 执行要求

从本文件落地起，后续仓库开发默认遵循本规范。若规范与实际代码形态冲突，应在修正代码或更新文档之间做出明确选择，而不是放任两者长期漂移。